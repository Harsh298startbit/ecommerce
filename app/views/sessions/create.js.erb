<% if @success %>
  // Show success notification
  showNotification('<%= j @message %>', 'success');
  
  // Redirect to chosen path after a short delay
  setTimeout(function() {
    window.location.href = '<%= j @redirect_path || root_path %>';
  }, 1000);
<% else %>
  // Show error notification
  showNotification('<%= j @message %>', 'error');
<% end %>
